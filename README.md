# CalmandCode-full-stack-app-development
A Node.js + Express food-ordering demo application with server-rendered EJS pages. It displays a categorized menu, product details, and a session-based shopping cart (add/remove/edit quantity) backed by a MySQL database and styled with Bootstrap. Orders are saved to the database and a simple checkout flow is provided.
